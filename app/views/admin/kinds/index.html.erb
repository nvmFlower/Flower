    <div class="card mb-3">
      <div class="card-header">
        <i class="fa fa-table"></i>&nbsp;<%= t "admin.categories.kind"%>
        <%= link_to t("admin.categories.add"), new_admin_kind_path,
          style: "float: right;" %>
      </div>
      <div class="card-body">
  <div class="table-responsive">
    <table class="table table-bordered" cellspacing="0">
      <thead>
        <tr>
          <th><%= t "admin.categories.index"%></th>
          <th><%= t ".name" %></th>
          <th><%= t "admin.categories.created_at" %></th>
          <th><%= t "admin.categories.updated_at" %></th>
          <th><%= t "admin.categories.change" %></th>
        </tr>
         <% @kinds.each_with_index do |kind, index| %>
          <tr>
            <td><%= create_index params[:page], index, Settings.page %></td>
            <td><%= kind.name %></td>
            <td><%= kind.created_at %></td>
            <td><%= kind.updated_at %></td>
            <td>
              <%= link_to  edit_admin_kind_path(kind) do %>
              <i class="material-icons" style="font-size:24px">sync</i>
            <% end %>
            <%= link_to [:admin, kind], method: :delete, data: { confirm: t("confrim") } do %>
              <i class="fa fa-remove" style="font-size:24px;color:red"></i>
            <% end %>
            </td>
          </tr>
         <% end %>
      </thead>
    </table>
     <%= paginate @kinds %>
  </div>
</div>

      </div>
